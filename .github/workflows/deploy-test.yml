# All the actions will be executed only if the commit happens on main branch
name: Deploy-Test
on:
  workflow_dispatch:

jobs:
  reuse:
    uses: B60-CloudDevOps/roboshop-github-actions/.github/workflows/deploy.yml@main
    with:
      app_type: nodejs
      component: catalogue
      app_env: test
# test